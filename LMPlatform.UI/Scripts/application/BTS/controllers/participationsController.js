angular
    .module('btsApp.ctrl.participations', ['ngTable'])
    .controller('participationsCtrl', [
        '$scope',
        'PAGE_SIZE',
        'NgTableParams',
        function ($scope, PAGE_SIZE, NgTableParams) {
            
        }]);
