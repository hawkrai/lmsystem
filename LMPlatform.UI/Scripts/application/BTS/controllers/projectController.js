angular
    .module('btsApp.ctrl.project', [])
    .controller('projectCtrl', [
        '$scope',
        '$routeParams',
        'projectsService',
        function ($scope, $routeParams, projectsService) {

        }]);
