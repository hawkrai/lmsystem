 
<li class="dropdown" style="margin-right: 10px">
    <a href="" class="dropdown-toggle" data-toggle="dropdown"><i class="fa fa-user"></i> @User.Identity.Name<b class="caret"></b></a>
    <ul class="dropdown-menu">
        @if (!User.IsInRole("admin"))
        {
            <li><a href="@Url.Action("Management", "Account")"><i class="fa fa-cogs"></i> Личный кабинет</a></li>
            <li><a href="#" class="msg-send" data-url="@Url.Action("WriteMessage", "Message", new { toadmin = true })"><i class="fa fa-cogs"></i> Написать администратору</a></li>   
            <li><a href="#"><i class="fa fa-info-circle"></i> О программе</a></li>     
        }
        <li><a href="javascript:document.getElementById('logoutForm').submit()"><i class="fa fa-power-off"></i> Выйти</a></li>
    </ul>
</li>
@using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new {id = "logoutForm"}))
            {
                @Html.AntiForgeryToken()
                
            }
