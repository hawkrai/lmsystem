@using Application.Core.UI.HtmlHelpers
@using LMPlatform.UI.Controllers
@{
    ViewBag.Title = "Управление предметами";
    Layout = "~/Views/Shared/Layouts/_MainUsingNavLectorLayout.cshtml";
}

@section scripts{
    @Scripts.Render("~/bundles/datatable")
    @Scripts.Render("~/bundle/subjectManagement")
}

@section navpanel
{
    Управление предметами
}

@*@section subnavbar
{
    <div class="panel panel-success">
        <div class="panel-heading">
            <h1 class="panel-title">Панель преподавателя</h1>
        </div>
        <div class="panel-body">
            @Html.ActionLink("Управление предметами", "Management", "Subject", null, new { @class = "btn btn-success btn-lms" })
            @Html.ActionLink("Сообщения", "Index", "Lms", null, new { @class = "btn btn-success btn-lms" })
            @Html.ActionLink("Управление новостями системы", "Index", "Lms", null, new { @class = "btn btn-success btn-lms" })
        </div>
    </div>
}*@

@section navleft
{
    <li><a href="#"><i class="fa fa-desktop"></i> Предметы</a></li>
    <li><a href="#"><i class="fa fa-desktop"></i> Предметы</a></li>
    <li><a href="#"><i class="fa fa-desktop"></i> Предметы</a></li>
    <li><a href="@Url.Action("Index","Lms")"><i class="fa fa-desktop"></i> Вернуться обратно</a></li>
}

<div style="margin: 20px">
    <div id="buttonActionSection">
        <a href="@Url.Action("Create", "Subject")" class="addSubject" data-toggle="tooltip"><i class="fa fa-plus-circle fa-2x"></i></a>
    </div>
    <div>
        @Html.Partial("_DataTable", new DataTableViewModel
        {
            DataTable = Html.DataTable("subjectList", (SubjectController controller) => controller.GetSubjects(null)),
            DataTableOptions = new DataTableOptions
            {
                Filterable = true,
                Info = true,
                OnComplete = "subjectManagement.subjectEditItemActionHandler",
                Pagination = true,
                Searchable = true,
                Sortable = false,
                
            }
        })    
    </div>
    
</div>

