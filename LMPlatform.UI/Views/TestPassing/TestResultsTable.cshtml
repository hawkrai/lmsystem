@using Application.Core.UI.HtmlHelpers
@using LMPlatform.UI.Controllers
@model int
<div>
        @Html.Partial("_DataTable", new DataTableViewModel
        {
            DataTable = Html.DataTable("testResultsList", (TestPassingController controller) => controller.GetTestsTesults(null, 0),
            new { groupId = Model}),
            DataTableOptions = new DataTableOptions
            {
                Filterable = true,
                Info = true,
                Pagination = false,
                Searchable = true,
                Sortable = false,
                OnComplete = "initTestResultsList"
            }
        })
    </div>